const CONTENT_TYPE = "Content-Type"
const ACCEPT = "Accept"
const AUTHORIZATION = "Authorization"
const X_ERROR_TYPE = "X-Error-Type"

const APPLICATION_JSON = "application/json"

export const httpHeaders = {
    CONTENT_TYPE,
    ACCEPT,
    AUTHORIZATION,
    X_ERROR_TYPE,
}

export const httpHeadersValue = {
    APPLICATION_JSON
}